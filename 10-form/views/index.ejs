<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>폼</title>
  <style>
    #id:required {
      border: 1px solid black;
    }

    #id:invalid {
      border: 1px solid red;
    }
  </style>
</head>

<body>
  <h1>
    <%= title %>
  </h1>

  <h2>Get 요청</h2>
  <!-- 유효성 검사는 나중에 추가하기 -->
  <form action="/login" method="get">
    <label for="id">ID</label>
    <input type="text" name="id" placeholder="ID는 최소 3글자" required minlength="3" id="id" />
    <label>
      PW
      <input type="password" name="pw" placeholder="PW는 최소 4글자" required minlength="0" />
    </label>
    <input type="text" name="email" placeholder="EMAIL은 필수값" required />
    <input type="number" name="grade" placeholder="GRADE" />
    <button>제출</button>
  </form>

  <h2>Post 요청</h2>
  <!-- required 속성: 폼 요소에 값이 반드시 들어가야하는지 여부를 결정 -->
  <!-- pattern 속성: 폼 요소에 값 입력되었을 때 그 입력된 값에 대한 검증을 의미 -->

  <!-- **required, pattern은 form validation 배우고 추가하기 -->
  <form action="/login" method="post">
    <input type="text" name="id" placeholder="ID" pattern="^([a-zA-Z0-9가-힣]){4,}$"
      title="아이디는 대소문자, 한글, 숫자로 구성된 4자 이상의 문자여야 합니다." required />
    <input type="password" name="pw" placeholder="PW" pattern="([a-z0-9]){8,12}"
      title="비밀번호는 영어 소문자와 숫자로 구성된 8자리 이상 12자리 이하의 문자여야 합니다." required />
    <button>제출</button>
  </form>

  <!-- js form 유효성 검사 -->
  <form action="/js-form-check" method="post" name="valiForm" onsubmit="return checkForm(this)">
    <input type="text" name="id" minlength="1" placeholder="ID" title="최소 1글자">
    <span>우편물 수령장소</span>
    <input type="checkbox" name="post" value="자택">
    <input type="checkbox" name="post" value="회사">
    <button>제출</button>
  </form>


  <script>
    function checkForm(form) {

      if (form.id.value == "") {

        form.id.focus();
        alert("아이디를 입력해주세요");

        return false;
      }

      if ((form.post[0].checked == false) && (form.post[1].checked == false)) {
        alert("우편물 수령 장소를 선택해 주십시오.");
        return false;
      }
    }
  </script>
</body>

</html>